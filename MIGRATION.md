# 数据迁移说明

## ⚠️ 重要提示

新版本改变了数据结构，从**单一状态**改成**多人投票模型**。

### 旧版数据结构
```json
{
  "530000": 2,
  "110000": 1
}
```
（每个地区只有一个状态值）

### 新版数据结构
```json
{
  "530000": {
    "小明": 2,
    "小红": 1
  },
  "110000": {
    "小李": 1
  }
}
```
（每个地区记录多个用户的投票）

## 🔄 自动迁移

首次打开新版本时，系统会自动将旧数据转换为新格式：
- 旧的标记会被归属到"匿名用户"名下
- 之后每个人输入自己的昵称就可以独立标记了

## 📌 注意事项

1. **首次使用需要输入昵称**（会弹出对话框）
2. **昵称会保存在浏览器**，下次自动登录
3. **如果多人同时使用旧版数据**，需要协商谁先升级，避免数据覆盖

## 🆕 新功能

- ✅ **多人投票统计**：鼠标悬停显示"3人标记：2票-很想去，1票-去过"
- ✅ **两级地图**：全国视图（34个省）→ 点击省份 → 进入该省城市视图
- ✅ **搜索功能**：输入省份或城市名，按回车定位
- ✅ **用户识别**：右上角显示当前用户名
- ✅ **面包屑导航**：点击"全国"返回

## 🐛 已修复

- 优化了地图粒度（初始只显示省级，避免太细）
- 改进了缩放体验
- 解决了"看不出谁点的"问题
